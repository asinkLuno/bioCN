[project]
name = "biocn"
version = "0.1.0"
description = "Chinese bionic reading EPUB processor with HanLP grammar analysis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click",
    "ebooklib",
    "beautifulsoup4",
    "hanlp>=2.1.3",
    "tqdm>=4.67.1",
    "loguru>=0.7.3",
]

[project.scripts]
biocn = "src.cli:cli"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.uv]
index-url = "https://mirrors.aliyun.com/pypi/simple/"

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple"
default = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "autoflake>=2.3.1",
    "basedpyright>=1.38.3",
    "isort>=7.0.0",
    "mdformat>=1.0.0",
    "pre-commit>=4.5.1",
    "radon>=6.0.1",
    "ruff>=0.14.8",
    "tool>=0.8.0",
    "uv>=0.12.10",
]
